
repositories{
    jcenter()
    flatDir{
        dirs 'libs'
    }
}

dependencies {

    implementation(name: 'openCVLibrary343-release', ext: 'aar')
    implementation(name: 'alive_detected_libary-release', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}